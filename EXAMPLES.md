# Modbus RTU 批处理工具使用示例

## 🚀 快速上手

### 示例1：最简单的使用

```bash
python modbus_rtu_test.py
```

**效果：**
- 读取从站0x01和0x02
- 使用COM66端口，921600波特率
- 最高频率批量读取
- 持续运行直到按Ctrl+C

**输出示例：**
```
串口: COM66 | 波特率: 921600 | 从站ID: [1, 2] | 频率: 最高 | 批次数: 无限 | 从站间延迟: 0.10ms
【批处理模式】批量发送请求，批量接收响应
按 Ctrl+C 退出

[从站 0x01] ACC:(  0.123,  9.456,  0.789)m/s² | QUAT:(0.9998,0.0012,0.0034,0.0056)
[从站 0x02] ACC:(  0.234,  9.567,  0.890)m/s² | QUAT:(0.9997,0.0023,0.0045,0.0067)
...
```

---

## 📊 常见场景

### 示例2：高速采集4个传感器

```bash
python modbus_rtu_test.py -s 1,2,3,4 -t 0
```

**说明：**
- `-s 1,2,3,4`：读取4个从站
- `-t 0`：无延迟，最快速度

**适用场景：**
- 高频数据采集
- 实时监控系统
- 性能测试

---

### 示例3：长时间监控并记录日志

```bash
python modbus_rtu_test.py -f 10 > sensor_data.log 2>&1
```

**说明：**
- `-f 10`：10Hz频率（每秒10批次）
- `> sensor_data.log`：输出重定向到文件
- `2>&1`：错误也记录到文件

**适用场景：**
- 长期数据记录
- 后续数据分析
- 故障追溯

---

### 示例4：定量测试

```bash
python modbus_rtu_test.py -s 1,2 -c 1000
```

**说明：**
- `-c 1000`：执行1000个批次后自动停止
- 自动显示统计信息

**适用场景：**
- 性能基准测试
- 可靠性测试
- 对比测试

---

### 示例5：自定义串口配置

```bash
python modbus_rtu_test.py -p COM3 -b 115200 -s 1,2,3
```

**说明：**
- `-p COM3`：使用COM3端口
- `-b 115200`：波特率115200
- `-s 1,2,3`：读取3个从站

**适用场景：**
- 不同硬件配置
- 多串口测试
- 兼容性测试

---

### 示例6：调整通信时序

```bash
python modbus_rtu_test.py -s 1,2 -t 0.001
```

**说明：**
- `-t 0.001`：从站间延迟1ms
- 适合响应较慢的设备

**适用场景：**
- 设备响应慢
- 通信不稳定
- 需要优化时序

---

## 🧪 测试工具

### 示例7：运行快速测试

```bash
python quick_test.py
```

**功能：**
- 自动运行5个测试场景
- 每个测试10秒或指定次数
- 测试间可控制

**测试列表：**
1. 默认配置（2从站，最高频率）
2. 单从站测试
3. 4从站批处理
4. 低频监控（1Hz）
5. 无延迟高速测试

---

### 示例8：性能对比测试

```bash
python performance_comparison.py -s 1,2,3,4 -c 100
```

**功能：**
- 对比传统循环模式
- 对比批处理模式
- 显示性能提升

**输出示例：**
```
================================================================================
性能对比结果
================================================================================
模式             总数     成功     失败     总耗时(s)    平均耗时(ms)     实际频率(Hz)
--------------------------------------------------------------------------------
传统循环模式     400      400      0        8.523        21.31           46.9
批处理模式       400      400      0        4.127        10.32           96.9
================================================================================

性能提升: 51.6%
加速比: 2.07x
批处理模式比传统模式快 2.07 倍
```

---

## 📈 高级用法

### 示例9：流式处理（配合其他工具）

```bash
python modbus_rtu_test.py -c 100 | grep "从站 0x01" | awk '{print $5}'
```

**功能：**
- 只提取从站0x01的数据
- 只显示加速度值
- 适合后续处理

---

### 示例10：多窗口同时监控

**终端1（实时显示）：**
```bash
python modbus_rtu_test.py
```

**终端2（记录日志）：**
```bash
python modbus_rtu_test.py -p COM67 > backup.log
```

**说明：**
- 两个串口同时工作
- 互不干扰
- 数据备份

---

## 🔧 故障排查示例

### 问题1：响应超时

**现象：**
```
[从站 0x01] 响应超时或不完整
[从站 0x02] 响应超时或不完整
```

**解决方案：**
```bash
# 增加从站间延迟
python modbus_rtu_test.py -t 0.002

# 降低频率
python modbus_rtu_test.py -f 50
```

---

### 问题2：解析失败

**现象：**
```
[从站 0x01] 解析失败
```

**解决方案：**
```bash
# 检查波特率
python modbus_rtu_test.py -b 115200

# 减少从站数量测试
python modbus_rtu_test.py -s 1
```

---

### 问题3：部分从站无响应

**现象：**
```
[从站 0x01] ACC:(...) | QUAT:(...)
[从站 0x02] 未找到响应帧
```

**解决方案：**
```bash
# 增加延迟
python modbus_rtu_test.py -t 0.005

# 单独测试问题从站
python modbus_rtu_test.py -s 2 -c 10
```

---

## 💡 最佳实践

### 1. 初次使用

```bash
# 先单从站测试
python modbus_rtu_test.py -s 1 -c 10

# 成功后测试所有从站
python modbus_rtu_test.py -s 1,2 -c 10

# 最后长时间运行
python modbus_rtu_test.py
```

### 2. 性能优化

```bash
# 从默认配置开始
python modbus_rtu_test.py -c 100

# 尝试无延迟
python modbus_rtu_test.py -t 0 -c 100

# 找到最佳配置
python modbus_rtu_test.py -t 0.0001 -c 100
```

### 3. 生产环境

```bash
# 稳定配置 + 日志记录
python modbus_rtu_test.py -f 100 -t 0.0002 >> data_$(date +%Y%m%d_%H%M%S).log 2>&1
```

---

## 📚 更多信息

- **完整文档**：查看 `MODBUS_USAGE.md`
- **项目说明**：查看 `README.md`
- **项目总结**：查看 `PROJECT_SUMMARY.md`
- **参数帮助**：运行 `python modbus_rtu_test.py -h`

---

**提示：** 所有示例中的串口、波特率、从站ID等参数都可以根据实际情况调整。
