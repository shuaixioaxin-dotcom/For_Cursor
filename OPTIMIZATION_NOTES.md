# IMU Modbus RTU 数据稳定性优化说明

## 问题分析

根据示波器观察，IMU1偶发收不到应答帧，可能原因：

1. **RS485收发切换时序不足** - 高波特率(921600)下，收发器切换需要更多时间
2. **无重试机制** - 单次通信失败直接退避，导致数据丢失
3. **帧间隔太短** - 多设备轮询时可能产生总线冲突
4. **响应解析脆弱** - 字节级验证可能误丢弃有效帧

## 优化措施

### 1. 增加RS485时序余量

```cpp
// 原参数
TX_ENABLE_DELAY_US = 30
TX_DISABLE_DELAY_US = 60
BUS_SILENCE_US = 500

// 优化后
TX_ENABLE_DELAY_US = 100    // 增加收发切换时间
TX_DISABLE_DELAY_US = 150   // 确保最后字节完全发送
BUS_SILENCE_US = 1000       // 增加总线静默时间
```

**原理**：RS485收发器在高波特率下切换方向时需要一定的建立时间。增加延迟可以确保：
- 发送前收发器完全切换到发送模式
- 发送后等待最后一个字节完全离开发送缓冲区
- 接收前收发器稳定在接收模式

### 2. 添加重试机制

```cpp
MAX_RETRIES = 2           // 每次请求最多重试2次
RETRY_DELAY_MS = 5        // 重试前延迟5ms
```

**状态机新增状态**：
- `STATE_PRE_REQUEST_DELAY` - 请求前延迟
- `STATE_RETRY_DELAY` - 重试前延迟

**好处**：
- 偶发通信失败不会立即标记为失败
- 通过重试可以恢复大部分临时性错误
- 统计信息中增加重试次数，便于分析问题

### 3. 增加请求间最小延迟

```cpp
MIN_REQUEST_GAP_MS = 2    // 请求间至少间隔2ms
```

**原理**：确保两个请求之间有足够的时间间隔，避免：
- IMU设备处理上一个响应时收到新请求
- 总线上残留信号干扰

### 4. 增加串口接收缓冲区

```cpp
Serial2.setRxBufferSize(512);  // 默认256，增加到512
```

**原理**：在高波特率下，较大的缓冲区可以：
- 避免数据溢出
- 给处理逻辑更多时间

### 5. 改进响应超时

```cpp
RESPONSE_TIMEOUT_MS = 150  // 原100ms -> 150ms
```

**原理**：给IMU设备更多处理时间，特别是在负载较高时。

### 6. 优化退避策略

```cpp
// 原退避基数：20ms
uint32_t backoff = 20 << fail_streak;

// 优化后：10ms
uint32_t backoff = 10 << fail_streak;
```

**原理**：由于增加了重试机制，可以减小退避时间，提高恢复速度。

## 实时性影响评估

| 指标 | 原版本 | 优化版本 | 变化 |
|------|--------|----------|------|
| 单次请求耗时 | ~1.1ms | ~1.5ms | +36% |
| 最大重试耗时 | 0 | ~15ms | - |
| 成功率（预期） | ~95% | ~99%+ | +4% |
| 平均频率影响 | - | -5~10% | 轻微下降 |

**权衡**：牺牲约5-10%的轮询频率，换取显著提升的数据稳定性。

## 调试建议

1. **启用DEBUG_RETRIES**：观察重试发生频率
   ```cpp
   static const bool DEBUG_RETRIES = true;
   ```

2. **检查统计信息**：关注以下指标
   - Retry count：重试次数
   - CRC Error count：CRC错误次数
   - Success Rate：成功率

3. **示波器检查点**：
   - DE/RE信号切换时序
   - 请求帧与响应帧间隔
   - 总线空闲状态

## 进一步优化方向

如果问题仍然存在，可以考虑：

1. **硬件方面**：
   - 检查RS485终端电阻（120Ω）
   - 检查总线布线，减少干扰
   - 增加去耦电容

2. **软件方面**：
   - 降低波特率到460800或更低
   - 实现自适应超时
   - 添加数据校验和滤波

3. **协议方面**：
   - 考虑使用广播查询模式
   - 实现数据缓存和平滑
