# IMU数据读取频率优化分析

## 当前性能瓶颈

### 1. 时间消耗计算（2个IMU，一个完整周期）

**单个IMU查询时间：**
- Modbus请求：8字节 @ 921600 baud = ~87μs
- Modbus响应：47字节 @ 921600 baud = ~510μs  
- 总线静默时间：500μs
- TX使能/禁用延迟：30μs + 60μs = 90μs
- **单次成功查询总计：~1.19ms**

**2个IMU完整周期：**
- 2 × 1.19ms = ~2.38ms
- **理论最大频率：~420Hz**

### 2. 实际限制因素

1. **CSV输出开销**：每个周期串口输出大量文本
2. **响应超时**：30ms超时时间过长
3. **频率报告**：每秒打印统计信息
4. **Arduino处理延迟**：主循环处理时间

## 优化策略

### 方案A：激进优化（目标200Hz+）

1. **减少寄存器读取数量**
   - 当前读22个寄存器（44字节）
   - 仅读加速度+四元数：11个寄存器（22字节）
   - 响应从47字节→27字节，节省~217μs

2. **提高波特率**（如果硬件支持）
   - 921600 → 2000000 baud
   - 通信时间减少约54%

3. **优化输出**
   - 降低CSV输出频率（每10个周期输出1次）
   - 或使用二进制输出格式
   - 减少频率报告频率（5秒1次）

4. **减少延迟**
   - 总线静默：500μs → 200μs
   - 响应超时：30ms → 10ms
   - TX延迟根据硬件调整

5. **预计效果**
   - 单个IMU：~0.6ms
   - 2个IMU周期：~1.2ms  
   - **目标频率：~800Hz**

### 方案B：保守优化（目标200Hz）

仅优化输出和超时，保持通信参数不变：
- 每5个周期输出1次CSV
- 频率报告改为5秒
- 超时10ms
- **预计频率：~300Hz**
