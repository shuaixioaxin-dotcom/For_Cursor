# 硬件故障排查指南

当软件优化后仍存在稳定性问题时，需要系统地排查硬件问题。

## 快速诊断流程图

```
数据不稳定？
    ↓
是否有数据输出？
    ├─ 否 → 【问题1：完全无通信】
    └─ 是 → 继续
         ↓
    超时多还是CRC错误多？
    ├─ 超时多 → 【问题2：设备响应慢/无响应】
    └─ CRC多 → 【问题3：数据损坏】
              ↓
         是否规律性出现？
         ├─ 规律 → 【问题4：系统性干扰】
         └─ 偶发 → 【问题5：偶发性干扰】
```

---

## 问题1：完全无通信

### 症状
- 串口只输出启动信息
- 无任何CSV数据
- 统计信息显示 Success=0

### 排查步骤

#### 1.1 检查电源供电
```
测试工具：万用表

ESP32：
- [ ] 3.3V引脚电压：应为3.2-3.4V
- [ ] 5V引脚电压（如使用）：应为4.8-5.2V
- [ ] GND连接正常

IMU设备：
- [ ] 电源电压符合规格（通常5V或12V）
- [ ] 电源纹波 <100mV
- [ ] 电流消耗正常（参考规格书）

RS485模块：
- [ ] VCC电压正常（3.3V或5V）
- [ ] 指示灯（如有）是否亮起
```

**解决方案**：
- 使用独立电源供电
- 添加去耦电容：100nF + 10uF
- 检查电源线接触是否良好

---

#### 1.2 检查RS485接线
```
标准接线：
ESP32          RS485模块       IMU1/IMU2
GPIO33(TX) →   DI
GPIO32(RX) ←   RO
GPIO25     →   DE/RE
GND        ─   GND        ─   GND
               A+         ─   A+
               B-         ─   B-
```

**检查项**：
- [ ] TX连接到DI（不是RO！）
- [ ] RX连接到RO（不是DI！）
- [ ] A-A, B-B正确连接（不能接反！）
- [ ] GND共地
- [ ] 无短路、断路

**测试方法**：
1. 万用表测量A、B间电阻：应为120-150Ω（含终端电阻）
2. 上电后测量A、B对GND电压：应不同（如+2.5V和-2.5V）
3. 用示波器观察A-B差分信号

**常见错误**：
- ❌ TX接到RO，RX接到DI → 无法通信
- ❌ A-B接反 → 无法通信
- ❌ DE/RE未连接或接错 → 无法切换发送/接收模式

---

#### 1.3 检查IMU设备配置
```
测试工具：Modbus测试软件（如Modbus Poll）

检查项：
- [ ] IMU设备ID正确（1和2）
- [ ] 波特率设置为921600
- [ ] 数据位8，停止位1，无校验（8N1）
- [ ] Modbus RTU模式（不是ASCII）
- [ ] 设备已正确初始化
```

**测试方法**：
使用USB转RS485单独测试每个IMU：
```cpp
// 单独测试IMU1
发送: 01 03 00 34 00 16 [CRC]
预期响应: 01 03 2C [数据...] [CRC]

// 单独测试IMU2
发送: 02 03 00 34 00 16 [CRC]
预期响应: 02 03 2C [数据...] [CRC]
```

**解决方案**：
- 使用IMU配置工具重新配置
- 恢复出厂设置后重新配置
- 确认固件版本正确

---

#### 1.4 检查ESP32引脚
```
测试工具：示波器或逻辑分析仪

检查项：
- [ ] GPIO33(TX)有输出波形
- [ ] GPIO25(DE/RE)有高低电平切换
- [ ] 波形完整，无毛刺
```

**测试代码**：
```cpp
void setup() {
  pinMode(25, OUTPUT);
  pinMode(33, OUTPUT);
  
  // 测试引脚切换
  while(1) {
    digitalWrite(25, HIGH);
    Serial2.write("TEST");
    delay(10);
    digitalWrite(25, LOW);
    delay(10);
  }
}
```

**解决方案**：
- 更换GPIO引脚
- 检查ESP32是否损坏
- 重新烧录程序

---

## 问题2：设备响应慢或无响应（超时多）

### 症状
- 统计信息显示 Timeout 数量多
- 偶尔有数据输出
- CRC错误较少

### 排查步骤

#### 2.1 增加响应超时时间
```cpp
// 尝试增加超时
static const uint16_t RESPONSE_TIMEOUT_MS = 200;  // 甚至300
```

**如果问题解决**：
- ✅ 说明IMU处理较慢，不是故障
- 💡 可以保持此设置

**如果问题未解决**：
- ⚠️ 继续排查

---

#### 2.2 检查总线负载
```
测试工具：示波器

观察项：
- [ ] 总线上是否有其他设备通信
- [ ] 是否有异常的突发信号
- [ ] 空闲时总线电平是否稳定
```

**问题特征**：
- 总线上有大量无关数据 → 设备ID冲突或有其他主机
- 总线不断跳变 → 设备故障或接线错误

**解决方案**：
- 断开其他设备，单独测试
- 检查是否有多个主机
- 确认每个从机ID唯一

---

#### 2.3 降低波特率
```cpp
// 尝试降低波特率
static const uint32_t RS485_BAUD = 460800;  // 或更低

// 记得同步修改IMU配置！
```

**适用场景**：
- 线缆较长（>20m）
- 环境EMI严重
- 使用低质量线缆

**效果评估**：
| 波特率 | 距离 | 抗干扰 | 速度 |
|--------|------|--------|------|
| 921600 | 50m | ★★ | ★★★★★ |
| 460800 | 100m | ★★★ | ★★★★ |
| 230400 | 200m | ★★★★ | ★★★ |
| 115200 | 500m | ★★★★★ | ★★ |

---

#### 2.4 检查IMU负载
```
测试工具：观察IMU行为

检查项：
- [ ] IMU是否同时响应其他请求
- [ ] IMU处理器负载是否过高
- [ ] IMU温度是否异常
- [ ] IMU固件是否有bug
```

**解决方案**：
- 更新IMU固件到最新版本
- 减少对IMU的其他操作
- 检查IMU配置是否合理

---

## 问题3：数据损坏（CRC错误多）

### 症状
- 统计信息显示 CRC Error 数量多
- 有数据输出但经常出错
- 超时较少

### 排查步骤

#### 3.1 检查终端电阻
```
RS485总线必须在两端安装120Ω终端电阻！

正确配置：
[ESP32]--RS485--[IMU1]----[IMU2]
   |                          |
  120Ω                      120Ω

错误配置：
- ❌ 没有终端电阻
- ❌ 只有一端有
- ❌ 中间节点有终端电阻
- ❌ 电阻值不对（如100Ω或150Ω）
```

**测试方法**：
1. 断开所有设备电源
2. 万用表测量A-B间电阻
   - 有两个120Ω并联：应测得约60Ω
   - 只有一个：约120Ω
   - 没有：>10kΩ（线缆电阻）

**解决方案**：
- 在总线两端焊接120Ω电阻（1/4W）
- 或使用内置终端电阻的RS485模块（需开启跳线）

---

#### 3.2 检查线缆质量
```
推荐线缆：
✅ 超五类/六类双绞线
✅ RS485专用线缆（带屏蔽）
✅ 工业总线电缆

不推荐线缆：
❌ 普通单芯线
❌ 杜邦线
❌ 太软或太细的线
```

**线缆特性**：
| 类型 | 阻抗 | 衰减 | 抗干扰 | 推荐距离 |
|------|------|------|--------|----------|
| 超五类双绞线 | 100Ω | 低 | ★★★★ | <100m |
| RS485专用线 | 120Ω | 很低 | ★★★★★ | <500m |
| 普通电线 | 不定 | 高 | ★ | <10m |

**检查项**：
- [ ] 线缆是否是双绞线（A/B绞在一起）
- [ ] 线缆长度是否过长
- [ ] 是否有接头或破损
- [ ] 是否与强电线缆平行走线

**解决方案**：
- 使用高质量双绞线
- 缩短线缆长度
- 避免与220V/380V线缆平行布线（间隔>30cm）

---

#### 3.3 检查接地与屏蔽
```
推荐接地方案：

方案A：单点接地（推荐）
[ESP32 GND]───[IMU1 GND]───[IMU2 GND]
      |
    [大地]

方案B：屏蔽线单端接地
[ESP32]══[屏蔽线]══[IMU1]══[屏蔽线]══[IMU2]
   |
[大地]
```

**检查项**：
- [ ] GND是否连接良好
- [ ] 是否有地环路（多点接地）
- [ ] 屏蔽线是否单端接地
- [ ] 接地点阻抗是否足够低

**常见错误**：
- ❌ 没有共地 → 差模干扰
- ❌ 多点接地 → 地环路电流
- ❌ 屏蔽层两端接地 → 环路噪声

**解决方案**：
- 确保所有设备GND连接
- 屏蔽线仅在一端（主机侧）接地
- 使用粗导线连接GND（减少阻抗）

---

#### 3.4 检查信号质量
```
测试工具：示波器

测量点：
1. RS485模块的A、B差分输出
2. 第一个IMU的A、B输入
3. 最后一个IMU的A、B输入

正常波形特征：
- 差分电压：±1.5V到±5V
- 上升/下降时间：<100ns（921600波特率）
- 无明显过冲、振铃
- 无噪声毛刺
```

**异常波形及原因**：

1. **波形衰减严重**（末端电压<1V）
   - 原因：线缆太长、质量差、无终端电阻
   - 解决：缩短线缆、降低波特率、检查终端电阻

2. **波形振铃**（多次上下震荡）
   - 原因：阻抗不匹配、终端电阻不对
   - 解决：使用120Ω标准终端电阻、改善线缆

3. **波形有毛刺/噪声**
   - 原因：EMI干扰、接地不良
   - 解决：加屏蔽、改善接地、远离干扰源

4. **波形畸变**（不规则）
   - 原因：总线冲突、多主机、设备故障
   - 解决：检查设备ID、断开测试

---

#### 3.5 增加总线保护
```
推荐保护电路：

1. TVS二极管（瞬态抑制）
   [A] ─┬─ TVS ─┬─ GND
   [B] ─┴─ TVS ─┘

2. 共模电感（滤波）
   [A_in] ─══─ [A_out]
              ││
   [B_in] ─══─ [B_out]

3. 隔离模块（彻底隔离）
   使用光耦隔离RS485模块
```

**适用场景**：
- 工业现场、强EMI环境
- 雷击风险区域
- 与其他系统互联

---

## 问题4：系统性干扰（规律性出错）

### 症状
- 每隔固定时间出错（如每秒一次）
- 某个特定IMU总是出错
- 特定时间段出错（如白天/晚上）

### 排查步骤

#### 4.1 识别干扰源
```
常见系统性干扰源：

电力系统：
- [ ] 继电器/接触器动作
- [ ] 电机启停
- [ ] 开关电源纹波
- [ ] 照明调光器

通信系统：
- [ ] WiFi/蓝牙定时发送
- [ ] 其他串口通信
- [ ] CAN/SPI总线活动

机械系统：
- [ ] 电磁阀动作
- [ ] 步进电机运行
- [ ] 伺服电机换向
```

**定位方法**：
1. 观察错误时间模式
2. 逐一关闭可疑设备
3. 使用频谱分析仪测量干扰频率

---

#### 4.2 空间隔离
```
最小间隔建议：

RS485线缆与强电线缆：
- 220V线缆：>30cm
- 380V线缆：>50cm
- 大功率电机：>100cm

RS485线缆与通信线缆：
- WiFi天线：>20cm
- 蓝牙模块：>15cm
- 其他信号线：>5cm（或交叉走线）
```

**改善措施**：
- 使用金属线槽分隔
- 交叉走线而非平行
- 缩短暴露在干扰中的距离

---

#### 4.3 时间隔离
```cpp
// 如果干扰有规律，可以时间错开

// 例如，避开WiFi发送时刻
void loop() {
  static uint32_t last_wifi_time = 0;
  
  // WiFi每秒发送一次，耗时约50ms
  if (millis() - last_wifi_time > 100) {
    // 安全时间窗口，进行IMU查询
    queryIMU();
  }
}
```

---

## 问题5：偶发性干扰（随机出错）

### 症状
- 错误随机出现，无规律
- 环境变化时情况不同
- 重试机制有效（Retry Success >0）

### 解决方案

#### 5.1 软件已优化（当前版本）
```
✅ 已实现的优化：
- 增加总线静默时间
- 优化TX切换延迟
- 快速重试机制
- 改进缓冲区清理

如果重试成功率 >80%：
- 说明优化有效，可接受
- 偶发干扰在容忍范围内
```

---

#### 5.2 进一步硬件改善
```
优先级从高到低：

1. 添加/检查终端电阻 ★★★★★
   - 最简单、最有效

2. 使用屏蔽线缆 ★★★★
   - 显著降低EMI影响

3. 改善接地 ★★★★
   - 降低共模干扰

4. 添加TVS保护 ★★★
   - 抑制瞬态干扰

5. 降低波特率 ★★★
   - 提高信号余量

6. 使用隔离模块 ★★
   - 彻底隔离，成本较高
```

---

## 综合诊断工具

### 自检程序
```cpp
// 在setup()中添加
void selfTest() {
  Serial.println("\n=== 硬件自检 ===");
  
  // 1. 引脚测试
  pinMode(RS485_DE_RE_PIN, OUTPUT);
  digitalWrite(RS485_DE_RE_PIN, HIGH);
  delay(10);
  digitalWrite(RS485_DE_RE_PIN, LOW);
  Serial.println("[OK] DE/RE引脚切换正常");
  
  // 2. 串口测试
  Serial2.begin(RS485_BAUD, SERIAL_8N1, RS485_RX_PIN, RS485_TX_PIN);
  Serial2.write("TEST");
  Serial.println("[OK] Serial2初始化正常");
  
  // 3. 单独测试每个IMU
  for (uint8_t i = 0; i < NUM_IMUS; i++) {
    Serial.printf("测试 IMU %d...", IMU_IDS[i]);
    
    // 发送请求
    sendRequest(IMU_IDS[i]);
    
    // 等待响应
    delay(RESPONSE_TIMEOUT_MS);
    
    if (Serial2.available() > 0) {
      Serial.println(" [OK] 有响应");
    } else {
      Serial.println(" [FAIL] 无响应");
    }
    
    delay(500);
  }
  
  Serial.println("=== 自检完成 ===\n");
}
```

### 信号质量监控
```cpp
// 添加到统计输出
void reportSignalQuality() {
  static uint32_t total_attempts = 0;
  static uint32_t total_success = 0;
  
  total_attempts++;
  if (imu_data[current_imu_index].valid) {
    total_success++;
  }
  
  float signal_quality = 100.0 * total_success / total_attempts;
  
  Serial.printf("信号质量: %.2f%% ", signal_quality);
  
  if (signal_quality > 99.5) {
    Serial.println("[优秀] ★★★★★");
  } else if (signal_quality > 98) {
    Serial.println("[良好] ★★★★");
  } else if (signal_quality > 95) {
    Serial.println("[一般] ★★★");
  } else if (signal_quality > 90) {
    Serial.println("[较差] ★★");
  } else {
    Serial.println("[很差] ★ 请检查硬件！");
  }
}
```

---

## 硬件检查清单

### 电源系统
- [ ] ESP32供电电压3.2-3.4V
- [ ] IMU供电符合规格
- [ ] 电源纹波 <100mV
- [ ] 去耦电容已安装（100nF + 10uF）
- [ ] 电流消耗正常

### 接线系统
- [ ] TX→DI, RX→RO
- [ ] A-A, B-B（未接反）
- [ ] GND共地
- [ ] 无短路、断路
- [ ] 接线牢固，无松动

### 终端电阻
- [ ] 总线两端各有120Ω
- [ ] 中间节点无终端电阻
- [ ] 测量A-B电阻约60Ω

### 线缆系统
- [ ] 使用双绞线或专用线缆
- [ ] 长度 <50m（921600波特率）
- [ ] 无破损、无接头
- [ ] 与强电线缆间隔 >30cm
- [ ] 屏蔽线单端接地

### 信号质量
- [ ] 差分电压 >1V
- [ ] 无明显振铃、过冲
- [ ] 无噪声毛刺
- [ ] 波形稳定

### EMI保护
- [ ] 远离电机、继电器
- [ ] 屏蔽良好
- [ ] 接地良好
- [ ] TVS保护（可选）

---

## 常用测试设备

### 基础工具（必备）
- **万用表**：测电压、电阻、通断
- **示波器**：观察波形（带宽 >10MHz）
- **USB转RS485**：单独测试IMU

### 进阶工具（推荐）
- **逻辑分析仪**：抓取数字信号
- **频谱分析仪**：定位干扰源
- **Modbus测试软件**：Modbus Poll/Slave

### 专业工具（可选）
- **协议分析仪**：深度分析Modbus通信
- **EMI测试设备**：测量电磁辐射
- **隔离探头**：高压测量安全

---

## 最终建议

### 标准部署配置
1. ✅ 使用超五类双绞线（带屏蔽）
2. ✅ 两端安装120Ω终端电阻
3. ✅ 所有GND连接，屏蔽层单端接地
4. ✅ 添加TVS保护二极管
5. ✅ 与强电线缆保持 >30cm距离
6. ✅ 线缆长度控制在50m以内

### 故障判断原则
```
如果重试成功率 >80%：
→ 偶发干扰，软件优化已足够，可接受

如果重试成功率 50-80%：
→ 检查硬件基础（接线、终端电阻、接地）

如果重试成功率 <50%：
→ 系统性问题，必须排查硬件
```

### 求助时需提供的信息
1. 硬件配置：ESP32型号、RS485模块型号、IMU型号
2. 接线图：拍照或绘制
3. 线缆信息：类型、长度
4. 环境信息：是否有强电设备、干扰源
5. 测试数据：统计信息截图、示波器波形
6. 已尝试的措施：列出所有尝试过的方法

---

**硬件是基础，软件是优化。两者结合才能达到最佳性能！**
