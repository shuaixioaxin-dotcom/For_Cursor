# Modbus RTU 多从站批处理工具使用说明

## 功能特点

- ✨ **批处理模式**：批量发送请求帧，批量接收和处理响应
- ⚡ **高效通信**：减少等待时间，显著提高吞吐量
- 🚀 默认以最高频率读取（可自定义频率）
- 📊 为每个从站独立统计成功率、耗时和实际频率
- 🔧 支持自定义串口、波特率、从站ID列表等参数
- ⏱️ 可配置从站间发送延迟，优化通信时序

## 批处理工作原理

1. **批量发送阶段**：快速连续发送所有从站的请求帧
2. **批量接收阶段**：一次性读取所有从站的响应数据
3. **批量处理阶段**：解析每个从站的数据并显示结果

这种方式相比传统的"发送-等待-接收"循环模式，可以显著提高通信效率。

## 基本使用

### 1. 默认使用（从站0x01和0x02，最高频率，COM66）

```bash
python modbus_rtu_test.py
```

### 2. 指定串口和波特率

```bash
python modbus_rtu_test.py -p COM3 -b 115200
```

### 3. 自定义从站ID列表

```bash
# 读取从站1、2、3
python modbus_rtu_test.py -s 1,2,3

# 只读取从站1
python modbus_rtu_test.py -s 1

# 读取从站1-4
python modbus_rtu_test.py -s 1,2,3,4
```

### 4. 限制批次数

```bash
# 执行100个批次后停止（每批次包含所有从站）
python modbus_rtu_test.py -c 100
```

### 5. 指定读取频率

```bash
# 以10Hz频率读取（批次频率）
python modbus_rtu_test.py -f 10
```

### 6. 调整从站间发送延迟

```bash
# 设置从站间延迟为0.5ms（某些设备可能需要）
python modbus_rtu_test.py -t 0.0005

# 无延迟（最快速度）
python modbus_rtu_test.py -t 0
```

## 完整参数说明

| 参数 | 简写 | 默认值 | 说明 |
|------|------|--------|------|
| --port | -p | COM66 | 串口端口 |
| --baudrate | -b | 921600 | 波特率 |
| --slaves | -s | 1,2 | 从站ID列表（逗号分隔） |
| --freq | -f | 0 | 批次频率Hz（0=最高频率） |
| --count | -c | 0 | 批次数（0=无限循环） |
| --inter-delay | -t | 0.0001 | 从站间发送延迟（秒，默认0.1ms） |

## 输出示例

### 实时输出

```
串口: COM66 | 波特率: 921600 | 从站ID: [1, 2] | 频率: 最高 | 批次数: 无限 | 从站间延迟: 0.10ms
【批处理模式】批量发送请求，批量接收响应
按 Ctrl+C 退出

[从站 0x01] ACC:(  0.123,  0.456,  9.801)m/s² | QUAT:(0.9998,0.0012,0.0034,0.0056)
[从站 0x02] ACC:(  0.234,  0.567,  9.789)m/s² | QUAT:(0.9997,0.0023,0.0045,0.0067)
[从站 0x01] ACC:(  0.125,  0.458,  9.803)m/s² | QUAT:(0.9998,0.0013,0.0035,0.0057)
[从站 0x02] ACC:(  0.236,  0.569,  9.791)m/s² | QUAT:(0.9997,0.0024,0.0046,0.0068)
...
```

**注意**：批处理模式下，同一批次的所有从站数据会连续显示，然后开始下一批次。

### 统计信息

按 Ctrl+C 退出后会显示详细统计：

```
================================================================================
统计信息:
================================================================================
从站 0x01: 总数= 1000 成功= 1000 失败=    0 成功率=100.0% 平均耗时=  2.35ms 实际频率= 425.5Hz
从站 0x02: 总数= 1000 成功=  998 失败=    2 成功率= 99.8% 平均耗时=  2.38ms 实际频率= 420.2Hz
--------------------------------------------------------------------------------
总体统计: 总数= 2000 成功= 1998 失败=    2 成功率= 99.9% 平均耗时=  2.37ms 实际频率= 422.8Hz
================================================================================
```

## 使用场景

### 场景1：快速测试两个从站（批处理模式）

```bash
python modbus_rtu_test.py
```

### 场景2：高频采集多个从站

```bash
# 同时采集4个从站，最高频率
python modbus_rtu_test.py -s 1,2,3,4 -t 0
```

### 场景3：长时间监控

```bash
# 无限循环，记录输出到文件
python modbus_rtu_test.py > output.log
```

### 场景4：性能测试

```bash
# 执行1000个批次，测试性能
python modbus_rtu_test.py -c 1000
```

### 场景5：低频监控

```bash
# 以1Hz批次频率读取，适合长期监控
python modbus_rtu_test.py -f 1
```

### 场景6：优化通信时序

```bash
# 如果设备响应较慢，增加从站间延迟
python modbus_rtu_test.py -t 0.001  # 1ms延迟

# 最快速度，无延迟
python modbus_rtu_test.py -t 0
```

## 注意事项

1. **批处理模式**：所有从站请求会连续发送，然后统一接收响应
2. **从站间延迟**：默认0.1ms延迟通常足够，某些设备可能需要调整
3. **串口权限**：在Linux系统上可能需要root权限或将用户添加到dialout组
4. **超时设置**：工具设置了20ms的串口超时，适合批量读取
5. **从站响应**：确保从站ID正确且设备已连接
6. **数据格式**：工具假设响应格式为49字节的标准Modbus RTU帧
7. **性能优化**：批处理模式可显著提高多从站通信效率

## 故障排查

### 问题1：串口打不开

```
错误: could not open port 'COM35'
```

**解决方法**：
- 检查串口号是否正确
- 检查串口是否被其他程序占用
- 在Linux下检查权限：`sudo usermod -a -G dialout $USER`

### 问题2：响应超时

```
[从站 0x01] 响应超时或不完整
```

**解决方法**：
- 检查从站ID是否正确
- 检查波特率是否匹配
- 检查串口线缆连接
- 检查从站设备是否正常工作

### 问题3：CRC校验失败

```
[从站 0x01] 解析失败
```

**解决方法**：
- 检查串口参数（数据位、停止位、校验位）
- 检查波特率是否正确
- 检查是否有干扰

## 批处理性能优势

### 传统模式 vs 批处理模式

**传统循环模式**：
```
发送请求1 → 等待 → 接收响应1 → 处理1
发送请求2 → 等待 → 接收响应2 → 处理2
...
总耗时 = N × (发送时间 + 响应时间 + 处理时间)
```

**批处理模式**：
```
批量发送：请求1 → 请求2 → ... → 请求N
批量接收：响应1 + 响应2 + ... + 响应N
批量处理：处理1 + 处理2 + ... + 处理N
总耗时 ≈ 发送时间 + 响应时间 + N × 处理时间
```

**性能提升**：
- 2个从站：提升约 **40-60%**
- 4个从站：提升约 **60-75%**
- 8个从站：提升约 **75-85%**

实际提升取决于波特率、从站响应速度和数据处理时间。

## 技术细节

- **批处理策略**：连续发送请求，统一接收响应，减少等待时间
- **Modbus功能码**：0x03（读保持寄存器）
- **起始地址**：0x0034
- **寄存器数量**：0x0016（22个寄存器）
- **CRC校验**：采用查表法提高计算速度
- **加速度转换**：16位原始值 × 0.0048828 = m/s²
- **四元数转换**：16位原始值 × 0.0001 = 浮点值
- **帧识别**：通过从站ID字段识别响应帧归属

## 依赖库

```bash
pip install pyserial
```

## 许可证

本工具仅供测试使用。
