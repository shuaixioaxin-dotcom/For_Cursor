# Modbus RTU IMU 读取频率优化分析

## 当前参数分析

### 通信参数
- 波特率: 921600 bps (8N1 = 每字节10位)
- 请求帧: 8 字节
- 响应帧: 1(地址) + 1(功能码) + 1(字节数) + 44(数据) + 2(CRC) = 49 字节
- 单次通信总字节: 57 字节

### 理论传输时间计算
```
传输时间 = 57 字节 × 10 位/字节 ÷ 921600 bps = 0.62 ms
```

### 当前延迟开销
| 延迟项 | 时间 | 说明 |
|--------|------|------|
| TX_ENABLE_DELAY_US | 30 μs | RS485发送使能延迟 |
| TX_DISABLE_DELAY_US | 60 μs | RS485发送禁用延迟 |
| BUS_SILENCE_US | 500 μs | 总线静默等待 |
| Serial.flush() | ~0.09 ms | 等待发送完成 |

### 当前单次IMU通信理论时间
```
0.62ms (传输) + 0.03ms (TX使能) + 0.06ms (TX禁用) + 0.5ms (静默) ≈ 1.21 ms
```

### 2个IMU的理论频率
```
1000 ms ÷ (2 × 1.21 ms) ≈ 413 Hz
```

## 主要瓶颈识别

### 1. 串口输出阻塞 (最大瓶颈!)
`outputCycleCsv()` 每个周期都执行，使用 `Serial.print()` 进行文本格式化输出。

单次CSV输出估算：
- 每个IMU输出约 50 字符
- 2个IMU约 100 字符
- 在 2Mbps 波特率下: 100 × 10 / 2000000 = 0.5 ms
- **加上格式化开销，实际可能达 2-5 ms**

### 2. 不必要的延迟
- `BUS_SILENCE_US = 500 μs` 过于保守
- 可以减少到 100-200 μs

### 3. 响应超时设置
- `RESPONSE_TIMEOUT_MS = 30 ms` 正常情况不影响，但失败时会显著降低频率

## 优化方案

### 方案1: 减少串口输出频率 (推荐，简单有效)
- 不要每个周期都输出CSV
- 改为按固定频率输出（如100Hz或50Hz）
- 内部维持高频采样

### 方案2: 使用二进制协议输出
- 用二进制格式替代文本CSV
- 大幅减少数据量和格式化开销

### 方案3: 减少延迟参数
- BUS_SILENCE_US: 500 → 100 μs
- TX_ENABLE_DELAY_US: 30 → 10 μs
- TX_DISABLE_DELAY_US: 60 → 20 μs
- RESPONSE_TIMEOUT_MS: 30 → 5 ms

### 方案4: 异步串口输出
- 使用环形缓冲区
- 在空闲时间输出数据

## 200Hz 目标可行性分析

要达到200Hz，每个完整周期需要 ≤ 5ms：
- 2个IMU，每个IMU通信时间需要 ≤ 2.5ms
- 当前理论通信时间 ~1.2ms，有足够余量
- **关键是减少串口输出开销**

## 结论

**最有效的优化：减少或优化串口输出**

达到200Hz是完全可行的，主要需要：
1. 降低CSV输出频率
2. 或使用二进制输出
3. 减少保守的延迟参数
