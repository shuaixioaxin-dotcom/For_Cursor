# 快速开始指南

## 安装依赖

```bash
pip install pyserial
```

## 基本使用

### 1. 单个IMU测试（推荐新手）

```bash
python modbus_rtu_test.py -p COM66 -i 1 -c 100 -v
```

**参数说明：**
- `-p COM66`: 串口号（Windows用COMx，Linux用/dev/ttyUSBx）
- `-i 1`: 从站ID为1
- `-c 100`: 读取100次后停止
- `-v`: 显示详细错误信息（调试时使用）

**预期输出：**
```
串口: COM66 | 波特率: 921600 | 从站ID: [0x01]
响应超时: 10ms | 发送后等待: 0.2ms | 从站间延时: 0ms
频率: 最高 | 次数: 100
按 Ctrl+C 退出

[ID:0x01] ACC:( -0.049,  0.024,  9.815) m/s² | 1.23ms
[ID:0x01] ACC:( -0.053,  0.029,  9.810) m/s² | 1.21ms
[ID:0x01] ACC:( -0.048,  0.025,  9.820) m/s² | 1.22ms
...

================================================================================
统计信息:
================================================================================
[ID:0x01] 总数=100 成功=100 失败=0 成功率=100.0% 平均耗时=1.22ms 频率=819.7Hz
--------------------------------------------------------------------------------
[总计] 循环次数=100 总请求=100 成功=100 失败=0 成功率=100.0%
```

### 2. 多个IMU轮询测试

```bash
python modbus_rtu_test.py -p COM66 -i 1,2,3 -c 50
```

**预期输出：**
```
串口: COM66 | 波特率: 921600 | 从站ID: [0x01,0x02,0x03]
...

[ID:0x01] ACC:( -0.049,  0.024,  9.815) m/s² | 1.23ms
[ID:0x02] ACC:(  0.012, -0.031,  9.805) m/s² | 1.24ms
[ID:0x03] ACC:( -0.008,  0.018,  9.825) m/s² | 1.22ms
[ID:0x01] ACC:( -0.051,  0.026,  9.812) m/s² | 1.21ms
[ID:0x02] ACC:(  0.010, -0.033,  9.808) m/s² | 1.23ms
[ID:0x03] ACC:( -0.009,  0.019,  9.822) m/s² | 1.22ms
...

================================================================================
统计信息:
================================================================================
[ID:0x01] 总数=50 成功=50 失败=0 成功率=100.0% 平均耗时=1.22ms 频率=819.7Hz
[ID:0x02] 总数=50 成功=50 失败=0 成功率=100.0% 平均耗时=1.23ms 频率=813.0Hz
[ID:0x03] 总数=50 成功=50 失败=0 成功率=100.0% 平均耗时=1.22ms 频率=819.7Hz
--------------------------------------------------------------------------------
[总计] 循环次数=50 总请求=150 成功=150 失败=0 成功率=100.0%
```

### 3. 固定频率采样

```bash
python modbus_rtu_test.py -p COM66 -i 1 -f 500 -c 1000
```

**说明：** 以500Hz频率采样，共采集1000次（耗时2秒）

### 4. 连续范围从站

```bash
python modbus_rtu_test.py -p COM66 -i 1-5 -c 20
```

**说明：** 读取从站ID 1到5（共5个设备）

### 5. 无限循环模式（Ctrl+C停止）

```bash
python modbus_rtu_test.py -p COM66 -i 1
```

**说明：** 持续读取，直到手动停止

## 常见场景

### 场景1：检查IMU是否正常工作

```bash
python modbus_rtu_test.py -p COM66 -i 1 -c 10 -v
```

**判断标准：**
- ✅ 成功率100%：IMU工作正常
- ⚠️ 成功率90-99%：可能有干扰，检查线缆
- ❌ 成功率<90%：硬件问题或参数不正确

### 场景2：测试多IMU系统稳定性

```bash
python modbus_rtu_test.py -p COM66 -i 1-10 -c 100 -d 1
```

**参数：**
- `-i 1-10`: 10个IMU
- `-d 1`: 每个IMU之间延时1ms（防止总线冲突）

**判断标准：**
- 每个IMU成功率应 >98%
- 如果某个IMU成功率低，检查该设备

### 场景3：性能压力测试

```bash
python modbus_rtu_test.py -p COM66 -i 1 -f 1000 -c 10000
```

**参数：**
- `-f 1000`: 尝试达到1000Hz
- `-c 10000`: 测试10000次（耗时10秒）

**判断标准：**
- 平均耗时应 <1ms
- 成功率应 >99.5%
- 频率应接近1000Hz

### 场景4：故障排查

```bash
python modbus_rtu_test.py -p COM66 -i 1 -c 100 -v -t 50 -w 1
```

**参数：**
- `-v`: 显示详细错误
- `-t 50`: 增加超时到50ms
- `-w 1`: 增加发送后等待到1ms

**观察：**
- 查看错误类型分布
- 检查是否有CRC错误（线路问题）
- 检查是否有超时（波特率问题）

## 输出说明

### 实时数据行
```
[ID:0x01] ACC:( -0.049,  0.024,  9.815) m/s² | 1.23ms
│         │    │        │        │            │
│         │    │        │        │            └─ 本次读取耗时
│         │    │        │        └────────────── AccZ（重力方向约9.8m/s²）
│         │    │        └─────────────────────── AccY
│         │    └──────────────────────────────── AccX
│         └───────────────────────────────────── 加速度数据
└─────────────────────────────────────────────── 从站ID
```

### 统计信息
```
[ID:0x01] 总数=100 成功=100 失败=0 成功率=100.0% 平均耗时=1.22ms 频率=819.7Hz
│         │        │        │       │             │              │
│         │        │        │       │             │              └─ 理论最高频率
│         │        │        │       │             └──────────────── 平均单次耗时
│         │        │        │       └────────────────────────────── 成功率
│         │        │        └────────────────────────────────────── 失败次数
│         │        └─────────────────────────────────────────────── 成功次数
│         └──────────────────────────────────────────────────────── 总请求次数
└────────────────────────────────────────────────────────────────── 从站ID
```

## 参数完整说明

| 参数 | 短格式 | 说明 | 默认值 | 推荐值 |
|------|--------|------|--------|--------|
| `--port` | `-p` | 串口端口 | COM66 | 根据实际情况 |
| `--baudrate` | `-b` | 波特率 | 921600 | 921600 |
| `--ids` | `-i` | 从站ID列表 | 1 | 1 或 1,2,3 或 1-5 |
| `--freq` | `-f` | 读取频率Hz | 0（最高） | 0-1000 |
| `--count` | `-c` | 循环次数 | 0（无限） | 测试用100-1000 |
| `--delay` | `-d` | 从站间延时ms | 0 | 多设备时1-5 |
| `--timeout` | `-t` | 响应超时ms | 10 | 10-20 |
| `--tx-wait` | `-w` | 发送后等待ms | 0.2 | 0.2-1 |
| `--verbose` | `-v` | 详细信息 | 关闭 | 调试时开启 |

## 故障排查速查表

| 现象 | 可能原因 | 解决方法 |
|------|----------|----------|
| 成功率<50% | 波特率不匹配 | 检查设备波特率配置 |
| 频繁CRC错误 | 线路干扰/接触不良 | 检查RS485线缆和接头 |
| 偶发超时 | 总线负载高 | 增加超时 `-t 20` 或降低频率 |
| 延迟抖动大 | 系统负载高 | 关闭其他程序 |
| 多设备冲突 | 从站间延时不足 | 增加延时 `-d 2` |
| 持续超时 | 设备未响应 | 检查设备供电和ID配置 |

## 性能基准

### 单设备性能（921600波特率）

| 项目 | 数值 |
|------|------|
| 最低延迟 | 0.8ms |
| 平均延迟 | 1.2ms |
| 理论最高频率 | 5000Hz |
| 实际最高频率 | 800-1000Hz |
| 成功率 | >99.5% |

### 多设备性能（3个设备，无间隔）

| 项目 | 数值 |
|------|------|
| 单次循环耗时 | 3.6ms |
| 循环频率 | 278Hz |
| 单设备频率 | 92Hz |
| 成功率 | >98% |

## 下一步

1. **基础测试**：运行单设备测试，确保通信正常
2. **性能测试**：运行高频测试，了解系统极限
3. **稳定性测试**：运行长时间测试（如10000次），验证可靠性
4. **集成应用**：参考代码，集成到你的应用程序中

## 技术支持

遇到问题？检查：
1. README.md - 详细文档
2. CHANGELOG.md - 版本变更记录
3. 使用 `-v` 参数查看详细错误信息
